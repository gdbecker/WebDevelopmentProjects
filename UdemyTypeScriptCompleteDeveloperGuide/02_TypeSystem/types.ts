// TypeScript examples
const today = new Date();
today.getMonth();

const person = {
  age: 20,
};

class Color {
  name: string;

  constructor(name: string) {
    this.name = name;
  }
}

const red = new Color("red");
console.log(red);
